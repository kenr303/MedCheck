{
  "name": "medcheck-price-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "refresh": "wrangler dev --local & sleep 3 && curl -X POST http://localhost:8787/refresh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.78.0"
  }
}
